// Copyright 2019-present Facebook Inc. All rights reserved.
// This source code is licensed under the Apache 2.0 license found
// in the LICENSE file in the root directory of this source tree.

// Code generated by ent, DO NOT EDIT.

package entv2

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/entc/integration/migrate/entv2/predicate"
	"entgo.io/ent/entc/integration/migrate/entv2/zoo"
	"entgo.io/ent/schema/field"
)

// ZooQuery is the builder for querying Zoo entities.
type ZooQuery struct {
	config
	ctx        *QueryContext
	order      []zoo.OrderOption
	inters     []Interceptor
	predicates []predicate.Zoo
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ZooQuery builder.
func (q *ZooQuery) Where(ps ...predicate.Zoo) *ZooQuery {
	q.predicates = append(q.predicates, ps...)
	return q
}

// Limit the number of records to be returned by this query.
func (q *ZooQuery) Limit(limit int) *ZooQuery {
	q.ctx.Limit = &limit
	return q
}

// Offset to start from.
func (q *ZooQuery) Offset(offset int) *ZooQuery {
	q.ctx.Offset = &offset
	return q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (q *ZooQuery) Unique(unique bool) *ZooQuery {
	q.ctx.Unique = &unique
	return q
}

// Order specifies how the records should be ordered.
func (q *ZooQuery) Order(o ...zoo.OrderOption) *ZooQuery {
	q.order = append(q.order, o...)
	return q
}

// First returns the first Zoo entity from the query.
// Returns a *NotFoundError when no Zoo was found.
func (q *ZooQuery) First(ctx context.Context) (*Zoo, error) {
	nodes, err := q.Limit(1).All(setContextOp(ctx, q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{zoo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (q *ZooQuery) FirstX(ctx context.Context) *Zoo {
	node, err := q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Zoo ID from the query.
// Returns a *NotFoundError when no Zoo ID was found.
func (q *ZooQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = q.Limit(1).IDs(setContextOp(ctx, q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{zoo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (q *ZooQuery) FirstIDX(ctx context.Context) int {
	id, err := q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Zoo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Zoo entity is found.
// Returns a *NotFoundError when no Zoo entities are found.
func (q *ZooQuery) Only(ctx context.Context) (*Zoo, error) {
	nodes, err := q.Limit(2).All(setContextOp(ctx, q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{zoo.Label}
	default:
		return nil, &NotSingularError{zoo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (q *ZooQuery) OnlyX(ctx context.Context) *Zoo {
	node, err := q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Zoo ID in the query.
// Returns a *NotSingularError when more than one Zoo ID is found.
// Returns a *NotFoundError when no entities are found.
func (q *ZooQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = q.Limit(2).IDs(setContextOp(ctx, q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{zoo.Label}
	default:
		err = &NotSingularError{zoo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (q *ZooQuery) OnlyIDX(ctx context.Context) int {
	id, err := q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Zoos.
func (q *ZooQuery) All(ctx context.Context) ([]*Zoo, error) {
	ctx = setContextOp(ctx, q.ctx, ent.OpQueryAll)
	if err := q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Zoo, *ZooQuery]()
	return withInterceptors[[]*Zoo](ctx, q, qr, q.inters)
}

// AllX is like All, but panics if an error occurs.
func (q *ZooQuery) AllX(ctx context.Context) []*Zoo {
	nodes, err := q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Zoo IDs.
func (q *ZooQuery) IDs(ctx context.Context) (ids []int, err error) {
	if q.ctx.Unique == nil && q.path != nil {
		q.Unique(true)
	}
	ctx = setContextOp(ctx, q.ctx, ent.OpQueryIDs)
	if err = q.Select(zoo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (q *ZooQuery) IDsX(ctx context.Context) []int {
	ids, err := q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (q *ZooQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, q.ctx, ent.OpQueryCount)
	if err := q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, q, querierCount[*ZooQuery](), q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (q *ZooQuery) CountX(ctx context.Context) int {
	count, err := q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (q *ZooQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, q.ctx, ent.OpQueryExist)
	switch _, err := q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("entv2: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (q *ZooQuery) ExistX(ctx context.Context) bool {
	exist, err := q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ZooQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (q *ZooQuery) Clone() *ZooQuery {
	if q == nil {
		return nil
	}
	return &ZooQuery{
		config:     q.config,
		ctx:        q.ctx.Clone(),
		order:      append([]zoo.OrderOption{}, q.order...),
		inters:     append([]Interceptor{}, q.inters...),
		predicates: append([]predicate.Zoo{}, q.predicates...),
		// clone intermediate query.
		sql:  q.sql.Clone(),
		path: q.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (q *ZooQuery) GroupBy(field string, fields ...string) *ZooGroupBy {
	q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ZooGroupBy{build: q}
	grbuild.flds = &q.ctx.Fields
	grbuild.label = zoo.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (q *ZooQuery) Select(fields ...string) *ZooSelect {
	q.ctx.Fields = append(q.ctx.Fields, fields...)
	sbuild := &ZooSelect{ZooQuery: q}
	sbuild.label = zoo.Label
	sbuild.flds, sbuild.scan = &q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ZooSelect configured with the given aggregations.
func (q *ZooQuery) Aggregate(fns ...AggregateFunc) *ZooSelect {
	return q.Select().Aggregate(fns...)
}

func (q *ZooQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range q.inters {
		if inter == nil {
			return fmt.Errorf("entv2: uninitialized interceptor (forgotten import entv2/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, q); err != nil {
				return err
			}
		}
	}
	for _, f := range q.ctx.Fields {
		if !zoo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("entv2: invalid field %q for query", f)}
		}
	}
	if q.path != nil {
		prev, err := q.path(ctx)
		if err != nil {
			return err
		}
		q.sql = prev
	}
	return nil
}

func (q *ZooQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Zoo, error) {
	var (
		nodes = []*Zoo{}
		_spec = q.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Zoo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Zoo{config: q.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (q *ZooQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := q.querySpec()
	_spec.Node.Columns = q.ctx.Fields
	if len(q.ctx.Fields) > 0 {
		_spec.Unique = q.ctx.Unique != nil && *q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, q.driver, _spec)
}

func (q *ZooQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(zoo.Table, zoo.Columns, sqlgraph.NewFieldSpec(zoo.FieldID, field.TypeInt))
	_spec.From = q.sql
	if unique := q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if q.path != nil {
		_spec.Unique = true
	}
	if fields := q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, zoo.FieldID)
		for i := range fields {
			if fields[i] != zoo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (q *ZooQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(q.driver.Dialect())
	t1 := builder.Table(zoo.Table)
	columns := q.ctx.Fields
	if len(columns) == 0 {
		columns = zoo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if q.sql != nil {
		selector = q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if q.ctx.Unique != nil && *q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range q.predicates {
		p(selector)
	}
	for _, p := range q.order {
		p(selector)
	}
	if offset := q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ZooGroupBy is the group-by builder for Zoo entities.
type ZooGroupBy struct {
	selector
	build *ZooQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (zgb *ZooGroupBy) Aggregate(fns ...AggregateFunc) *ZooGroupBy {
	zgb.fns = append(zgb.fns, fns...)
	return zgb
}

// Scan applies the selector query and scans the result into the given value.
func (zgb *ZooGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, zgb.build.ctx, ent.OpQueryGroupBy)
	if err := zgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ZooQuery, *ZooGroupBy](ctx, zgb.build, zgb, zgb.build.inters, v)
}

func (q *ZooGroupBy) sqlScan(ctx context.Context, root *ZooQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(q.fns))
	for _, fn := range q.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*q.flds)+len(q.fns))
		for _, f := range *q.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*q.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := q.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ZooSelect is the builder for selecting fields of Zoo entities.
type ZooSelect struct {
	*ZooQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (zs *ZooSelect) Aggregate(fns ...AggregateFunc) *ZooSelect {
	zs.fns = append(zs.fns, fns...)
	return zs
}

// Scan applies the selector query and scans the result into the given value.
func (zs *ZooSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, zs.ctx, ent.OpQuerySelect)
	if err := zs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ZooQuery, *ZooSelect](ctx, zs.ZooQuery, zs, zs.inters, v)
}

func (q *ZooSelect) sqlScan(ctx context.Context, root *ZooQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(q.fns))
	for _, fn := range q.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*q.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := q.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
