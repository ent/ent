// Copyright 2019-present Facebook Inc. All rights reserved.
// This source code is licensed under the Apache 2.0 license found
// in the LICENSE file in the root directory of this source tree.

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/entc/integration/ent/socialprofile"
	"entgo.io/ent/entc/integration/ent/user"
)

// SocialProfile is the model entity for the SocialProfile schema.
type SocialProfile struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Desc holds the value of the "desc" field.
	Desc string `json:"desc,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SocialProfileQuery when eager-loading is set.
	Edges                SocialProfileEdges `json:"edges"`
	user_social_profiles *int
	selectValues         sql.SelectValues
}

// SocialProfileEdges holds the relations/edges for other nodes in the graph.
type SocialProfileEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SocialProfileEdges) UserOrErr() (*User, error) {
	if e.loadedTypes[0] {
		if e.User == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.User, nil
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SocialProfile) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case socialprofile.FieldID:
			values[i] = new(sql.NullInt64)
		case socialprofile.FieldDesc:
			values[i] = new(sql.NullString)
		case socialprofile.ForeignKeys[0]: // user_social_profiles
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SocialProfile fields.
func (sp *SocialProfile) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case socialprofile.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sp.ID = int(value.Int64)
		case socialprofile.FieldDesc:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field desc", values[i])
			} else if value.Valid {
				sp.Desc = value.String
			}
		case socialprofile.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_social_profiles", value)
			} else if value.Valid {
				sp.user_social_profiles = new(int)
				*sp.user_social_profiles = int(value.Int64)
			}
		default:
			sp.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SocialProfile.
// This includes values selected through modifiers, order, etc.
func (sp *SocialProfile) Value(name string) (ent.Value, error) {
	return sp.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the SocialProfile entity.
func (sp *SocialProfile) QueryUser() *UserQuery {
	return NewSocialProfileClient(sp.config).QueryUser(sp)
}

// Update returns a builder for updating this SocialProfile.
// Note that you need to call SocialProfile.Unwrap() before calling this method if this SocialProfile
// was returned from a transaction, and the transaction was committed or rolled back.
func (sp *SocialProfile) Update() *SocialProfileUpdateOne {
	return NewSocialProfileClient(sp.config).UpdateOne(sp)
}

// Unwrap unwraps the SocialProfile entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sp *SocialProfile) Unwrap() *SocialProfile {
	_tx, ok := sp.config.driver.(*txDriver)
	if !ok {
		panic("ent: SocialProfile is not a transactional entity")
	}
	sp.config.driver = _tx.drv
	return sp
}

// String implements the fmt.Stringer.
func (sp *SocialProfile) String() string {
	var builder strings.Builder
	builder.WriteString("SocialProfile(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sp.ID))
	builder.WriteString("desc=")
	builder.WriteString(sp.Desc)
	builder.WriteByte(')')
	return builder.String()
}

// SocialProfiles is a parsable slice of SocialProfile.
type SocialProfiles []*SocialProfile
